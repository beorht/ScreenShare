# 📌 Этапы разработки проекта "Запись экрана и трансляция по локальной сети"

## Этап 1. Подготовка окружения

1. Установить **OBS Studio** на Linux.
2. Подключить **OBS WebSocket API** (или плагин obs-websocket).
3. Установить инструменты разработки:

   * компилятор (**g++/clang**),
   * система сборки (**CMake**),
   * библиотеки (**Boost**, **FFmpeg**, **libobs**, при необходимости).
4. Поднять тестовый веб-сервер (**nginx**, либо простой http-сервер на C++).

---

## Этап 2. Захват экрана

1. Настроить в OBS источник **Screen Capture**.
2. Управление OBS через C++:

   * подключение к OBS WebSocket,
   * отправка команд `StartRecording` / `StopRecording`.
3. Альтернативный путь:

   * использовать **X11/XCB** или **Wayland API** для захвата экрана напрямую в C++,
   * передавать данные в **FFmpeg** для кодирования.

---

## Этап 3. Кодирование видео

1. Получить поток с OBS в формате **RTMP/RTSP**.
2. Если идем без OBS: использовать **FFmpeg (libavcodec, libavformat)** в C++ для кодирования:

   * кодек **H.264 (libx264)**,
   * контейнер **MPEG-TS** или **MP4**.
3. Обеспечить сжатие и минимальную задержку.

---

## Этап 4. Передача по локальной сети

1. Выбрать протокол передачи:

   * **HLS (HTTP Live Streaming)** – поддержка браузерами, но задержка 3–10 сек.
   * **MJPEG** – просто, но большой битрейт.
   * **WebRTC** – сложно, но минимальная задержка (0.5–1 сек).
2. Реализовать сервер на C++:

   * вариант 1: **Boost.Asio** (HTTP/HTTPS сервер),
   * вариант 2: встроенный **nginx + модуль HLS** (C++ только управляет).
3. Организовать отдачу потока клиентам (несколько подключений).

---

## Этап 5. Веб-клиент

1. Создать простую страницу `index.html` с `<video>` тегом.
2. Для HLS: подключить **hls.js** и указывать `stream.m3u8`.
3. Для WebRTC: использовать JavaScript API для подключения к медиасерверу.
4. Проверить воспроизведение на разных устройствах (ПК, телефон).

---

## Этап 6. Консольное приложение на C++

1. Реализовать интерфейс команд:

   * `./screenstream start` – запускает OBS или FFmpeg и сервер,
   * `./screenstream stop` – останавливает,
   * `./screenstream status` – показывает текущее состояние.
2. Добавить параметры запуска:

   * `--port` – порт для веб-сервера,
   * `--quality` – качество потока (битрейт),
   * `--fps` – кадровая частота.

---

## Этап 7. Тестирование и оптимизация

1. Проверить нагрузку на процессор и сеть.
2. Измерить задержку (HLS ≈ 5 сек, WebRTC ≈ 0.5 сек).
3. Проверить подключение нескольких клиентов одновременно.
4. Настроить оптимальный битрейт для локальной сети.

---

## Этап 8. Дополнительные улучшения

1. Сделать авторизацию (логин/пароль) для доступа к трансляции.
2. Добавить логирование действий (`log.txt`).
3. Возможность сохранять трансляцию в файл на сервере.
4. Реализовать автозапуск сервера при старте Linux.

---

# 📊 Сводная таблица

| Этап                     | Задачи                                                   | Инструменты/Технологии                   |
| ------------------------ | -------------------------------------------------------- | ---------------------------------------- |
| 1. Подготовка            | Установка OBS, компилятора, библиотек                    | OBS, CMake, g++, Boost, FFmpeg           |
| 2. Захват экрана         | Настройка Screen Capture, управление OBS или X11         | OBS WebSocket API, X11/XCB               |
| 3. Кодирование видео     | Кодек H.264, RTMP/RTSP или FFmpeg                        | libobs, FFmpeg (libavcodec, libavformat) |
| 4. Передача по сети      | Реализация стриминга (HLS/MJPEG/WebRTC)                  | Boost.Asio, nginx, hls.js                |
| 5. Веб-клиент            | HTML-страница с `<video>`                                | HTML5, hls.js, WebRTC API                |
| 6. Консольное приложение | Управление start/stop/status, параметры                  | C++, Boost, CLI                          |
| 7. Тестирование          | Проверка задержки, нагрузки, многопользовательский режим | Linux, браузеры                          |
| 8. Улучшения             | Авторизация, логирование, запись в файл                  | C++, OpenSSL (для HTTPS), systemd        |

---

Хочешь, я дальше сделаю **блок-схему архитектуры** проекта (как OBS → C++ сервер → браузер-клиент), чтобы было более наглядно?
